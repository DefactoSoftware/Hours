- projects.each do |project|
  .container
    %h3= project.name
    = render "entries", entries: project.entries, project: project
    %button.submit-billable-entries= t("billables.buttons.bill_selected")
