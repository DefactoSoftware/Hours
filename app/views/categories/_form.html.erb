<%= simple_form_for(@new_category) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, required: true, autofocus: true %>
    <%= f.button :button, data: { disable_with: loader(text: t("loader.saving")) } %>
  </div>
<% end %>
