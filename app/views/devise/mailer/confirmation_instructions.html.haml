.outer
  .container
    %p= t('email.confirmation.salutation', email: @email)
    %p= t('email.confirmation_text')
    %p= link_to t('email.confirmation.confirm_password'), confirmation_url(@resource, confirmation_token: @token, subdomain: current_subdomain)
